# Network Speed Monitor

A lightweight command-line Python script that measures network latency and internet bandwidth speeds using ICMP pings and automated speed tests.


## Requirements

* Python 3.x
* `ping3` library
* `speedtest-cli` library

## Installation

1. Clone or download this repository.
2. Install the required dependencies:

```bash
pip install ping3 speedtest-cli
